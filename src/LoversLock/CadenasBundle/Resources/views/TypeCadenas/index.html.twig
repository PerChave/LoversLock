{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TypeCadenas list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Accessibilite</th>
                <th>Prix</th>
                <th>Urlimage</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('typecadenas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.accessibilite }}</td>
                <td>{{ entity.prix }}</td>
                <td>{{ entity.uRLImage }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('typecadenas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('typecadenas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('typecadenas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
